{
  "name": "purescript-hertz",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "build": "run-s build:rake build:tsc build:pulp build:examples",
    "build:rake": "rake",
    "build:tsc": "tsc",
    "build:pulp": "pulp build --include examples -- --codegen corefn,js",
    "build:examples": "cd examples && rake"
  },
  "dependencies": {
    "inferno": "^6.2.1",
    "inferno-vnode-flags": "^6.2.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.3",
    "parcel-bundler": "^1.10.3",
    "typescript": "^3.1.6"
  }
}
